import React from 'react';
import { Typography } from '@material-ui/core';

export default () => (
  <div>
    <Typography variant="h5">Second Step</Typography>
    <Typography variant="body1">This is the second step!</Typography>
  </div>
);
