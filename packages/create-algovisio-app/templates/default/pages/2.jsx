import React from 'react';
import { Typography } from '@material-ui/core';

export default () => (
  <div>
    <Typography variant="h5">Last Step</Typography>
    <Typography variant="body1">This is the last step. Start creating now!</Typography>
  </div>
);
